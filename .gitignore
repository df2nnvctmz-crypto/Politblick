# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Regenerated from public/data/*.json at build time (see scripts/build-sitemap.mjs) — committing
# it would just add a noisy diff on every ~4h data-fetch commit for no benefit.
public/sitemap.xml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Promo video renders — large binaries that would sit in git history forever and are not part of
# the site build. Keep them locally or in release assets instead.
marketing/
